version: "3.9"

services:

  backend:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    depends_on:
      - milvus

  milvus:
    image: milvusdb/milvus:v2.4.4
    container_name: milvus
    ports:
      - "19530:19530"
      - "9091:9091"